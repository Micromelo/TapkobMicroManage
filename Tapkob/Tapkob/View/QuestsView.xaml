<UserControl x:Class="Tapkob.View.QuestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:Tapkob.ViewModel" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <ImageBrush Stretch="Fill"
                        ImageSource="/Resources/Icons/background.png"
                        AlignmentY="Top"
                        AlignmentX="Center"></ImageBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="6*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,0">
            <Button x:Name="PraporQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_prapor.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="TherapistQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_therapist.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="SkierQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_skier.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="PeacekeeperQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_peacekeeper.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="MechanicQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_mechanic.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="RagmanQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_ragman.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="JaegerQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_jaeger.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="FenceQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_fence.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
            <Button x:Name="LightkeeperQuestButton" 
                    Command="{Binding ChangeTraderCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Name}"
                    Cursor="Hand">
                <Image Source="/Resources/Icons/icon_trader_lightkeeper.png"
                       Width="Auto"
                       Height="Auto">
                </Image>
            </Button>
        </StackPanel>

        <!--// Main Tasks View //-->
        <Grid Width="395" 
              Grid.Row="1"
              HorizontalAlignment="Left">

            <!--// Complete Task Button //-->
            <Button x:Name="CompleteQuestButton" 
                    Height="60"
                    Margin="5,5,5,5"
                    VerticalAlignment="Top"
                    Cursor="Hand">
                <TextBlock Text="COMPLETE"
                           FontFamily="/Resources/Fonts/#Bender"
                           FontSize="15"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center">
                </TextBlock>
            </Button>

            <!--// Show Completed Tasks //-->
            <Grid Background="#902E333A"
                  Height="18"
                  Margin="5,68,5,0"
                  VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <CheckBox></CheckBox>
                    <TextBlock Text="Show Completed"
                               Foreground="#FFFFFF"
                               FontFamily="/Resources/Fonts/#Bender"
                               FontSize="12"
                               Margin="5,0,0,0">
                    </TextBlock>
                </StackPanel>
            </Grid>


            <!--// Task List //-->
            <ListView ItemsSource="{Binding TraderTasks}"
                      SelectedItem="{Binding SelectedTask}"
                      Background="Transparent"
                      BorderThickness="0"
                      VerticalAlignment="Stretch"
                      ScrollViewer.CanContentScroll="False"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Margin="5,90,5,5" 
                      d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </Style>
                </ListView.Resources>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView,AncestorLevel=1},Path=ActualWidth}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                            <ColumnDefinition Width="90"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Border Background="#902E333A"
                                                Grid.Column="0"
                                                MinHeight="80"
                                                Margin="0,0,5,0">
                                            <StackPanel Orientation="Horizontal"
                                                        HorizontalAlignment="Left">
                                                <Image Source="{Binding TaskIconPath}"
                                                       Grid.Column="0"
                                                       Height="24"
                                                       Width="24"
                                                       Margin="5">
                                                </Image>
                                                <TextBlock Text="{Binding Name}" 
                                                           Foreground="#FFFFFF"
                                                           FontFamily="/Resources/Fonts/#Bender"
                                                           FontSize="15"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"
                                                           Margin="5">
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#902E333A"
                                                Grid.Column="1"
                                                MinWidth="30"
                                                MinHeight="80">
                                            <TextBlock Text="{Binding IsCompleted}" 
                                                       TextWrapping="Wrap"
                                                       Foreground="#FFFFFF"
                                                       FontFamily="/Resources/Fonts/#Bender"
                                                       FontSize="15"
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center">
                                            </TextBlock>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <!--// Task Information Grid //-->
        <Grid Grid.Row="1"
              Margin="400,0,0,0"
              HorizontalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Background="#902E333A"
                      Grid.Row="0"
                      VerticalAlignment="Stretch"
                      Margin="5,5,10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    
                    <Image Source="{Binding SelectedTask.TaskIconPath}"
                           Grid.Column="0"
                           Height="24"
                           Width="24"
                           Margin="5">
                    </Image>
                    
                    <TextBlock  VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                Grid.Column="1">
                         <Hyperlink NavigateUri="{Binding SelectedTask.WikiLink}" 
                                    RequestNavigate="Hyperlink_RequestNavigate"
                                    TextDecorations="None">
                             <TextBlock Text="{Binding SelectedTask.Name}"
                                        Foreground="#FFFFFF"
                                        FontFamily="/Resources/Fonts/#Bender"
                                        FontSize="25"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Margin="0,5">
                             </TextBlock>
                         </Hyperlink>
                    </TextBlock>
                    <TextBlock Text="{Binding SelectedTask.Map.Name, FallbackValue='Any Location'}"
                               Grid.Column="2"
                               Foreground="#FFFFFF"
                               FontFamily="/Resources/Fonts/#Bender"
                               FontSize="15"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Margin="0,0,35,0">
                    </TextBlock>
                    <TextBlock Text="Completed!"
                               Grid.Column="3"
                               Foreground="#FFFFFF"
                               FontFamily="/Resources/Fonts/#Bender"
                               FontSize="15"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Margin="0,0,25,0">
                    </TextBlock>
                </Grid>

                <TextBlock Text="OBJECTIVE(S)"
                           Grid.Row="1"
                           Foreground="#FFFFFF"
                           FontFamily="/Resources/Fonts/#Bender"
                           FontSize="20"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Left"
                           Margin="5,0,0,0">
                </TextBlock>

                <ListView ItemsSource="{Binding SelectedTaskObjectiveDescriptions}"
                          Background="Transparent"
                          BorderThickness="0"
                          Grid.Row="2"
                          ScrollViewer.CanContentScroll="False"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Margin="0,5,15,5" d:ItemsSource="{d:SampleData ItemCount=2}">

                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </ListView.Resources>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <Grid Background="{TemplateBinding Background}">
                                            <Border Name="Selection" Visibility="Collapsed" />
                                            <!-- This is used when GridView is put inside the ListView -->
                                            <GridViewRowPresenter Grid.RowSpan="2"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView,AncestorLevel=1},Path=ActualWidth}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#952E333A"
                                                MinHeight="80">
                                            <StackPanel Orientation="Horizontal"
                                                        HorizontalAlignment="Center">
                                                <Image Source="{Binding ObjectiveIconPath}"
                                                       Height="24"
                                                       Width="24"
                                                       Margin="5">
                                                </Image>
                                                <TextBlock Text="{Binding Description}" 
                                                           TextWrapping="Wrap"
                                                           TextAlignment="Center"
                                                           Foreground="#FFFFFF"
                                                           FontFamily="/Resources/Fonts/#Bender"
                                                           FontSize="15"
                                                           Margin="10,5,10,0"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center">
                                                </TextBlock>
                                                <TextBlock Text="{Binding SubDescription}"
                                                           TextWrapping="Wrap"
                                                           TextAlignment="Center"
                                                           Foreground="#FFFFFF"
                                                           FontFamily="/Resources/Fonts/#Bender"
                                                           FontSize="15"
                                                           Margin="10,5,10,0"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center" 
                                                           FontWeight="Bold" 
                                                           FontStyle="Italic">
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <TextBlock Text="NEEDED KEYS"
                           Grid.Row="3"
                           Foreground="#FFFFFF"
                           FontFamily="/Resources/Fonts/#Bender"
                           FontSize="20"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Left"
                           Margin="5,0,0,0">
                </TextBlock>

                <ListView ItemsSource="{Binding SelectedTaskNeededKeys}"
                          Background="#35000000"
                          BorderThickness="0"
                          Grid.Row="4"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Margin="0,5,15,5" d:ItemsSource="{d:SampleData ItemCount=0}">

                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </ListView.Resources>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"></Setter>
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <Grid Background="{TemplateBinding Background}">
                                            <Border Name="Selection" Visibility="Collapsed" />
                                            <!-- This is used when GridView is put inside the ListView -->
                                            <GridViewRowPresenter Grid.RowSpan="2"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#302E333A"
                                                MinHeight="120"
                                                MaxHeight="120"
                                                MinWidth="120"
                                                HorizontalAlignment="Stretch">
                                            <Grid Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=GridView,AncestorLevel=1},Path=ActualWidth}">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                </Grid.RowDefinitions>
                                                <Border Background="#05FFFFFF"
                                                        BorderBrush="#40FFFFFF"
                                                        BorderThickness="1"
                                                        CornerRadius="5"
                                                        Grid.Row="0"
                                                        Margin="10"
                                                        HorizontalAlignment="Center">
                                                    <Image Source="{Binding BaseImageLink}"
                                                           Grid.Row="0"
                                                           Height="48"
                                                           Width="48"
                                                           Margin="5">
                                                    </Image>
                                                </Border>
                                                
                                                <TextBlock Grid.Row="1"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Center">
                                                    <Hyperlink NavigateUri="{Binding WikiLink}" 
                                                               RequestNavigate="Hyperlink_RequestNavigate"
                                                               TextDecorations="None">
                                                        <TextBlock Text="{Binding Name}"
                                                                   Grid.Row="1"
                                                                   TextWrapping="Wrap"
                                                                   TextAlignment="Center"
                                                                   VerticalAlignment="Center"
                                                                   HorizontalAlignment="Center"
                                                                   Foreground="#FFFFFF"
                                                                   FontFamily="/Resources/Fonts/#Bender"
                                                                   FontSize="15"
                                                                   Margin="10,0,10,10">  
                                                        </TextBlock>
                                                    </Hyperlink>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>

                </ListView>

                <TextBlock Text="REWARDS"
                           Grid.Row="5"
                           Foreground="#FFFFFF"
                           FontFamily="/Resources/Fonts/#Bender"
                           FontSize="20"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Left"
                           Margin="5,0,0,0">
                </TextBlock>

                <ListView ItemsSource="{Binding SelectedTaskRewards}"
                          Background="#35000000"
                          BorderThickness="0"
                          Grid.Row="6"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Margin="5,5,15,15" d:ItemsSource="{d:SampleData ItemCount=0}">

                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </ListView.Resources>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"></Setter>
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                                        <Grid Background="{TemplateBinding Background}">
                                            <Border Name="Selection" Visibility="Collapsed" />
                                            <!-- This is used when GridView is put inside the ListView -->
                                            <GridViewRowPresenter Grid.RowSpan="2"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#302E333A"
                                                MinHeight="140"
                                                MaxHeight="140"
                                                MinWidth="140"
                                                HorizontalAlignment="Stretch">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                    <RowDefinition Height="3*"></RowDefinition>
                                                    <RowDefinition Height="1*"></RowDefinition>
                                                </Grid.RowDefinitions>
                                                <Border Background="#05FFFFFF"
                                                        BorderBrush="#40FFFFFF"
                                                        BorderThickness="1"
                                                        MinHeight="48"
                                                        CornerRadius="5"
                                                        Grid.Row="0"
                                                        Margin="10"
                                                        HorizontalAlignment="Center">
                                                    <Image Source="{Binding BaseImageLink}"
                                                           Grid.Row="0"
                                                           MaxHeight="40"
                                                           MinWidth="48"
                                                           Margin="5">
                                                    </Image>
                                                </Border>
                                                <TextBlock Grid.Row="1"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Center">
                                                    <Hyperlink NavigateUri="{Binding WikiLink}" 
                                                               RequestNavigate="Hyperlink_RequestNavigate"
                                                               TextDecorations="None">
                                                        <TextBlock Text="{Binding Description}"
                                                                   Grid.Row="1"
                                                                   TextWrapping="Wrap"
                                                                   TextAlignment="Center"
                                                                   VerticalAlignment="Center"
                                                                   HorizontalAlignment="Center"
                                                                   Foreground="#FFFFFF"
                                                                   FontFamily="/Resources/Fonts/#Bender"
                                                                   FontSize="13"
                                                                   Margin="5,0,5,5" 
                                                                   FontWeight="Bold">  
                                                        </TextBlock>
                                                    </Hyperlink>
                                                </TextBlock>
                                                <TextBlock Text="{Binding SubDescription}"
                                                           Grid.Row="2"
                                                           TextWrapping="Wrap"
                                                           TextAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           HorizontalAlignment="Center"
                                                           Foreground="#FFFFFF"
                                                           FontFamily="/Resources/Fonts/#Bender"
                                                           FontSize="12"
                                                           FontStyle="Oblique"
                                                           Margin="5,0,5,5">
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
